
<!-- USAGE: PASTE THIS CODE INTO THE PAGE WHERE THE MODAL SHOULD BE DISPLAYED -->
<!-- ELEMENT ID MUST BE "open_warning" -->
<!-- <button class="btn-primary" id="open_video">Open video text</button> -->

<!-- The Modal -->
<div id="showvideo" class="video-modal">
  <!-- Modal content -->
  <div class="video-modal-content">
    <div class="video-modal-header">
      <span class="video-close" id="x-btn-close">&times;</span>
    </div>
    <div class="video-modal-body">
      <!-- <video src="https://youtu.be/37v96zMllFA?t=42"></video> -->
      <div id="player"></div>
      <!-- <iframe width="1000" height="600" src="https://www.youtube.com/embed/37v96zMllFA"></iframe> -->
    </div>
    <div class="video-modal-footer pt-2">
      <!-- <p>Keep this information for further reference</p> -->
      <div class="btn-info rounded" id="btn-video-close">Close</div>

    </div>
  </div>

</div>


<!-- 1. The <iframe> (and video player) will replace this <div> tag. -->


  <script>
    // 2. This code loads the IFrame Player API code asynchronously.
    var tag = document.createElement('script');

    tag.src = "https://www.youtube.com/iframe_api";
    var firstScriptTag = document.getElementsByTagName('script')[0];
    firstScriptTag.parentNode.insertBefore(tag, firstScriptTag);

    // 3. This function creates an <iframe> (and YouTube player)
    //    after the API code downloads.
    var player;
    function onYouTubeIframeAPIReady() {
      player = new YT.Player('player', {
        height: '550',
        width: '1000',
        videoId: '37v96zMllFA',
        playerVars: {
          'enablejsapi':'1',
          'autoplay': '1',
          'start': '1438',
          'end': '1511',
          'modestbranding':'1',
          'controls':'0',
          'showinfo':'0',
          'rel':'0',
          'origin':'http://localhost:3000'
        },
        events: {
          'onReady': onPlayerReady,
          'onStateChange': onPlayerStateChange
        }
      });
    }

    // 4. The API will call this function when the video player is ready.
    function onPlayerReady(event) {
      player.setPlaybackRate(2);
      player.setPlaybackQuality('hd720')
      event.target.playVideo();
    }

    // 5. The API calls this function when the player's state changes.
    //    The function indicates that when playing a video (state=1),
    //    the player should play for six seconds and then stop.
    var done = false;
    function onPlayerStateChange(event) {
      if (event.data == YT.PlayerState.PLAYING && !done) {
        setTimeout(stopVideo, 1200000);
        done = true;
      }
    }
    function stopVideo() {
      player.stopVideo();
    }
  </script>
